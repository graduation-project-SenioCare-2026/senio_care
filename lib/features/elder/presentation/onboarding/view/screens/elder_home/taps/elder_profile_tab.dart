import 'package:flutter/material.dart';

class ElderProfileTap extends StatelessWidget {
  const ElderProfileTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("elder profile"));
  }
}
