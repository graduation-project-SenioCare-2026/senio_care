import 'package:flutter/material.dart';

class SosTab extends StatelessWidget {
  const SosTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("sos"));
  }
}
